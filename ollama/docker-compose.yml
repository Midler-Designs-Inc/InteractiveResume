services:
  ollama:
    build: .
    container_name: ollama
    restart: unless-stopped
    mem_limit: 16g
    memswap_limit: 16g
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    stdin_open: true
    tty: true

volumes:
  ollama-data:
