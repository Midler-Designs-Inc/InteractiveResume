services:
  aider:
    build: ./
    container_name: aider
    tty: true
    stdin_open: true
    volumes:
      - ../:/workspace
    environment:
      - OLLAMA_API_BASE=http://host.docker.internal:11434
    command: ["--model", "qwen3-coder-next", "--yes-always", "--no-auto-commits"]